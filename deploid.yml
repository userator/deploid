name: deploid
releaseNameFormat: Y-m-d_H-i-s
chmod: 0777
structure:
  dirs:
    - releases
    - releases/first
    - shared
  files:
    - deploid.log
  links:
    - current:releases/first
mapping:
    releasesDir: releases
    logFile: deploid.log
    currentLink: current
    